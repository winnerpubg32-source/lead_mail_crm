from __future__ import annotations

# Admin registrations for the Analytics models are added together with the models
# in the Analytics phase. Keeping the module importable matters: Django loads it
# on every start via autodiscover.
