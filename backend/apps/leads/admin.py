from __future__ import annotations

# Admin registrations for the Leads models are added together with the models
# in the Leads phase. Keeping the module importable matters: Django loads it
# on every start via autodiscover.
