# ---------------------------------------------------------------------------
# OutreachOS — backend runtime dependencies (Phase 1 foundation)
# ---------------------------------------------------------------------------
Django==5.2.17
djangorestframework==3.18.1
django-cors-headers==4.9.0
django-environ==0.14.0
django-filter==26.1
drf-spectacular==0.30.0

# Database driver — PostgreSQL
psycopg[binary]==3.3.6

# Background processing — Redis broker + Celery workers
celery==5.6.3
redis==8.1.0

# Production WSGI server + static file serving
gunicorn==26.2.0
whitenoise==6.12.0
