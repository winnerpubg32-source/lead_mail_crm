from __future__ import annotations

# Admin registrations for the Email engine models are added together with the models
# in the Email engine phase. Keeping the module importable matters: Django loads it
# on every start via autodiscover.
