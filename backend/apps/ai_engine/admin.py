from __future__ import annotations

# Admin registrations for the AI engine models are added together with the models
# in the AI engine phase. Keeping the module importable matters: Django loads it
# on every start via autodiscover.
