from __future__ import annotations

# Admin registrations for the Contacts models are added together with the models
# in the Contacts phase. Keeping the module importable matters: Django loads it
# on every start via autodiscover.
